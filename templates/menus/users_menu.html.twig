<!-- Fixed navbar -->
	<div class="navbar navbar-inverse navbar-fixed-top headroom" >
		<div class="container">
			<div class="navbar-header">
				<!-- Button for smallest screens -->
				<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse"><span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </button>
				<a class="navbar-brand" href="{{ path('home_index') }}"><img src="{{asset('images/logo.png') }}" alt="Anawin-Mbomou"></a>
			</div>
			<div class="navbar-collapse collapse">
				<ul class="nav navbar-nav pull-right">
					<li class="{% if current_menu is defined and current_menu == 'home' %} active {% endif %}"><a href="{{ path('home_index') }}">Accueil</a></li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle " data-toggle="dropdown">Qui sommes-nous <b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li><a href="{{path('index_apropos')}}">Mot de la Directrice</a></li>
							<li><a href="{{path('index_apropos')}}">Qui sommes nous </a></li>
							
							<li class="active"><a href="{{ path('page') }}">L’équipe</a></li>
							<li class="active"><a href="{{ path('contact') }}">Nous Contacter</a></li>
						</ul>
					</li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">Nous connaitre  <b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li><a href="{{ path('page') }}">Programmes</a></li>
							<li class="active"><a href="{{ path('page') }}">Events</a></li>
						</ul>
					</li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">Documentation   <b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li><a href="{{path('articles_index')}}">Articles </a></li>
							<li><a href="{{ path('page') }}">Centrafrique</a></li>
							<li class="active"><a href="{{ path('page') }}">Pauvreté</a></li>
						</ul>
					</li>
					<li><a href="{{ path('page') }}">Galeries</a></li>
					<li class="{% if current_menu is defined and current_menu == 'contact' %} active {% endif %}"><a href="{{ path('contact') }}">Contact</a></li>
					
					{% if not app.user %}
						<li><a class="btn" href="{{ path('fos_user_security_login') }}">Connexion / Enregistrement</a></li>
					{% else %}
						<li><a class="btn btn-secondary" href="{{ path('fos_user_security_logout') }}"> LOG OUT</a></li>
						<li><a class="btn btn-secondary" href="{{ path('admin_home') }}" target="blank" >Admin</a></li>
					{% endif %}

				</ul>
			</div><!--/.nav-collapse -->
		</div>
	</div> 
	<!-- /.navbar -->